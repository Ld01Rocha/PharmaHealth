<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="20dp">

    <!-- BOTÃO VOLTAR -->

    <!-- TÍTULO -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_voltar"
        style="@style/Widget.MaterialComponents.Button.TextButton"
        android:layout_width="80dp"
        android:layout_height="46dp"
        android:icon="@android:drawable/ic_media_previous"
        android:insetLeft="0dp"
        android:insetTop="0dp"
        android:insetRight="0dp"
        android:insetBottom="0dp"
        android:padding="0dp"
        android:text="Voltar"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/tv_titulo_lista"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Meus Medicamentos"
        android:textSize="24sp"
        android:textStyle="bold"
        android:layout_marginStart="12dp"
        app:layout_constraintStart_toEndOf="@id/btn_voltar"
        app:layout_constraintTop_toTopOf="@id/btn_voltar"
        app:layout_constraintBottom_toBottomOf="@id/btn_voltar"
        app:layout_constraintEnd_toStartOf="@id/btn_add_medicamento" />

    <!-- BOTÃO ADICIONAR -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_add_medicamento"
        style="@style/Widget.MaterialComponents.Button.OutlinedButton"
        android:layout_width="104dp"
        android:layout_height="61dp"
        android:icon="@android:drawable/ic_input_add"
        android:padding="10dp"
        android:text="Adicionar"
        app:layout_constraintBottom_toBottomOf="@id/tv_titulo_lista"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@id/tv_titulo_lista" />

    <!-- RECYCLERVIEW OCUPANDO O RESTO DA TELA -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_medicamentos"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginTop="20dp"
        app:layout_constraintTop_toBottomOf="@id/tv_titulo_lista"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
         />

</androidx.constraintlayout.widget.ConstraintLayout>
